
&После("ПриНачалеРаботыСистемы")
Процедура МобилонПриНачалеРаботыСистемы()
	
	ТекущийПользователь = ВАТССервер.ВернутьТекущегоПользователя();
	ПоказыватьУведомленияПриЗвонках = ВАТССервер.ПолучитьЗначениеПараметра(ТекущийПользователь, "ПоказыватьУведомленияПриЗвонках");
	Если ЗначениеЗаполнено(ПоказыватьУведомленияПриЗвонках) И ПоказыватьУведомленияПриЗвонках Тогда
		ПодключитьОбработчикОжидания("ПоказатьФормуВходящегоЗвонка",5);	
	КонецЕсли;
	
КонецПроцедуры
